@model IEnumerable<MvcSA.Models.TD_Tell>
<table class="table table-bordered table-striped table-condensed">
    <thead>
        <tr>
            <th>From</th>
            <th>Message</th>
            <th>Date-Time</th>
            <th>Reply</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>@item.tell_user</td>
            <td>@item.tell_content</td>
            <td>@item.tell_dt</td>
            @if (ViewBag.IsIssuer == 1)
            {
                if (item.reply_dt != null)
                {
                    <td>@item.reply_dt</td>
                } else {
                    <td><a href="@Url.Action("ReplyTellIssuer")?tid=@item.tell_id" >Reply</a></td>
                }
            } else {
                <td>@item.reply_dt</td>
            }
        </tr>
        }
    </tbody>
</table>